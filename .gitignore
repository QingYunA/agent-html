# macOS
.DS_Store
*/.DS_Store
*/*/.DS_Store

# 过程产物：执行计划与 lint 基线记录。
# 保留在本地作为审计轨迹，但不随仓库分发、也不参与站点部署。
docs/

# 技能评测运行产物
agent-html-workspace/

# 宣传片用的授权音乐只在本地使用（曲库授权通常不允许单独分发音频文件）
scripts/promo/music/
